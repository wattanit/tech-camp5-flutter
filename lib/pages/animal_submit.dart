import 'package:flutter/material.dart';

class AnimalSubmitPage extends StatelessWidget {
  const AnimalSubmitPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("Animal Submit"),
    );
  }
}
