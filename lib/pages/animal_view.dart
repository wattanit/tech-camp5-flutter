import 'package:flutter/material.dart';

class AnimalViewPage extends StatelessWidget {
  const AnimalViewPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("Animal View Page"),
    );
  }
}
